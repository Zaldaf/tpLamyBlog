{% extends 'base.html.twig' %}

{% block title %}Hello CategorieController!{% endblock %}

{% block body %}
    <h1>Listes des catégorie</h1>
    <ul>
        {% for categorie in categories %}

            <li>{{ categorie.titre}} ({{ categorie.articles.count }} articles) <a href="{{ path('app_categorie_slug', {slug : categorie.slug}) }}" class="button">voir</a></li>

        {% endfor %}
    </ul>
{% endblock %}
